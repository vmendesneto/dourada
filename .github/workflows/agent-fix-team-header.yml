name: Validar workflow do header

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/agent-fix-team-header.yml

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: echo workflow-ok