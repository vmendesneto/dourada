name: Corrigir avatares do lobby

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/fix-lobby-avatars.yml
      - tool/fix_lobby_avatars.py

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Aplicar correção
        run: |
          python3 tool/fix_lobby_avatars.py
          git diff --check

      - name: Instalar dependências
        run: flutter pub get

      - name: Analisar
        run: flutter analyze

      - name: Testar
        run: flutter test

      - name: Gravar correção
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/ui/lobby_page.dart test/widget_test.dart
          git commit -m "Mostra fotos dos jogadores no lobby"
          git push origin HEAD:main
